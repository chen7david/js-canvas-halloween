# js-canvas-haloween

```js
ctx.drawImage(
    image,
    0, 0, // titlesheet x, y (top left corner of the grab)
    100, 100, // how big of a grab)
    0, 0, // Where you want the crop to be placed
    200, 200 // Side of the placement of what was grabbed
)
```